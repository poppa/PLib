<?php
try {
  $xslt = new XSLTransform('/path/to/template.xsl');
  $xslt->SetParam('date', strftime('%Y-%m-%d', time()));

  // $xmlObject can also be a string XML tree
  $res = $xsl->Transform($xmlObject);
  echo $res;
}
catch (XSLTransformException $e) {
  echo "Bugger: " . $e->getMessage();
  die;
}
?>